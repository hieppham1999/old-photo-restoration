# Core (required)
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
tqdm>=4.65.0

# Verify UI
gradio>=4.0.0

# Optional: LaMa inpainting (better quality for wide/long scratches)
# Enables the "lama" or "hybrid" inpainting modes.
#
# NOTE: simple-lama-inpainting 0.1.2 pins pillow<10.0.0, which downgrades
# Pillow and conflicts with gradio/scikit-image. The pin is overly strict —
# the package works fine with modern Pillow. Install in this order:
#
#   pip install torch torchvision      # match your CUDA: https://pytorch.org/get-started/locally/
#   pip install simple-lama-inpainting
#   pip install --upgrade "pillow>=10.1"   # restore Pillow after the forced downgrade
#
# torch>=2.0.0
# torchvision>=0.15.0
# simple-lama-inpainting
